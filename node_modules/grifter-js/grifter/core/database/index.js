var loggerdb  = require('./logging');
var datadb    = require('./data');

module.exports.configure = function (logger_mongo_url, data_mongo_url, callback) {
  loggerdb.configure(logger_mongo_url, function () {
    datadb.configure(data_mongo_url, callback);
  });
}



