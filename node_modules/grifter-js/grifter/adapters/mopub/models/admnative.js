var SchemaObject = require('schema-object');

/**
 Mopub: ADM Native
 */

var ADMNativeAsset = require('./imp_ext');

var definition = {
  native: {
    link: { type: [ String ] },
    imptrackers:{ [ String ] },
    assets: { type: [ ADMNativeAsset.definition ] }
  }
};

module.exports.definition = definition;
module.exports.schema = new SchemaObject(definition);;