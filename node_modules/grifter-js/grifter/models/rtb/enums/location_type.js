var Enum = require('enum');

/**
Location Type
The following table lists the options to indicate how the geographic information was determined.

*/

module.exports.LocationType = new Enum({
  'GPS/Location Services' : 1,
  'IP Address':             2,
  'User provided':          3
});
