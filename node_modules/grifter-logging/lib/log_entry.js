var mongoose = require('mongoose');

var definition = {
  timestamp:Date,
  data:{}
};

var logEntrySchema = new mongoose.Schema(definition);
var LogEntry = mongoose.model("LogEntry",logEntrySchema);

module.exports = LogEntry;